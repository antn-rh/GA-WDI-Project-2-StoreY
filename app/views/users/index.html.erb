<div class="col-md-6 col-md-offset-3">
  <h1> Store your memories here. <br> See what others around you are sharing. </h1>
</div>

<div class="transitions-enabled" id="pins">
  <% @users.each do |user| %>
  <div class="box panel panel-default">
    <%= link_to (image_tag user.stories.first.pins.first.image.url()), user_path(user) if user.stories.first && user.stories.first.pins.first && user.stories.first.pins.first.image? %>
    <div class="panel-body">
      <%= link_to user.name, user_path(user) %>
    </div>
  </div>
  <% end %>
</div>
